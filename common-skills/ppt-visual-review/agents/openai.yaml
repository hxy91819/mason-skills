interface:
  display_name: "PPT Visual Review"
  short_description: "委派审查与复验，主 Agent 直接修复视觉和内容问题，默认交付 HTML 前后对比报告"
  default_prompt: "Use $ppt-visual-review to delegate visual review and revalidation, directly fix actionable findings, and automatically deliver an HTML report using $review-html-report."

policy:
  allow_implicit_invocation: false
