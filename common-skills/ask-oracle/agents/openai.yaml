interface:
  display_name: "询问先知"
  short_description: "Investigate a focused technical question and consult an Oracle"
  default_prompt: "Use $ask-oracle to investigate this question, consult the configured Oracle through BB, and return its technical judgment."

policy:
  allow_implicit_invocation: true
