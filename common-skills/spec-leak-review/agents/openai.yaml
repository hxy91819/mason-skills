interface:
  display_name: "Spec Leak Review"
  short_description: "从产品体验视角审查界面文案，找出需求文本外泄"
  default_prompt: "Use $spec-leak-review to review a screen's copy and displayed elements for spec leak and produce an HTML report."

policy:
  allow_implicit_invocation: false
