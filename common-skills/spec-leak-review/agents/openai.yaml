interface:
  display_name: "Spec Leak Review"
  short_description: "审查并直接修复网站、幻灯片和文本的制作说明外泄，复验后默认交付 HTML 报告"
  default_prompt: "Use $spec-leak-review to find and directly fix spec leaks, revalidate the actual changes, and automatically deliver an HTML report using $review-html-report."

policy:
  allow_implicit_invocation: false
