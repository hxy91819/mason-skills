interface:
  display_name: "Auto Review"
  short_description: "Run a structured pre-commit or ship review"
  default_prompt: "Use $autoreview to review the requested change; default to a trusted BB session inside BB."
policy:
  allow_implicit_invocation: false
