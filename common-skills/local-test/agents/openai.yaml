interface:
  display_name: "Local Test Environment"
  short_description: "Set up, verify, and cleanly shut down isolated local test environments."
  default_prompt: "Use $local-test when setting up or running local multi-service testing environments for functional verification before deployment."

policy:
  allow_implicit_invocation: true
