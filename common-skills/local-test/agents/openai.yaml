interface:
  display_name: "Local Test Environment"
  short_description: "Manage safe, isolated local test environments with dev scripts and Nginx password protection."
  default_prompt: "Use $local-test to spin up a local development environment, run functional tests, and ensure clean shutdown."

policy:
  allow_implicit_invocation: true
