interface:
  display_name: "Local Test Environment"
  short_description: "Run the tech-stack-agnostic `lt` CLI (driven by a declarative .local-test.yml) to set up, verify, and cleanly shut down per-worktree isolated local test environments with shared middleware."
  default_prompt: "Use $local-test when setting up or running local multi-service testing environments for functional verification before deployment."

policy:
  allow_implicit_invocation: false
