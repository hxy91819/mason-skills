interface:
  display_name: "Large Task Orchestrator"
  short_description: "通过 BB 线程按 Story 难度派发 Worker，用廉价 Validator 确认完成并持续执行计划"
  default_prompt: "Use $large-task-orchestrator to drive this plan by dispatching BB worker threads via $bb-model-routing and cheap validator threads until delivery or a genuine blocker. Pick the worker difficulty from the Story; keep the orchestrator context to structured reports and diff stats."
policy:
  allow_implicit_invocation: false
