interface:
  display_name: "Large Task Orchestrator"
  short_description: "通过外部 Agent 编排执行大型 Story 计划"
  default_prompt: "Use $large-task-orchestrator to execute this large task plan through ACPX or Herdr."
policy:
  allow_implicit_invocation: false
