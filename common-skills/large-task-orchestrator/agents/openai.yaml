interface:
  display_name: "Large Task Orchestrator"
  short_description: "用宿主原生 subagent 持续执行大型任务计划"
  default_prompt: "Use $large-task-orchestrator to drive this plan with native worker and reviewer subagents until delivery or a genuine blocker."
policy:
  allow_implicit_invocation: false
