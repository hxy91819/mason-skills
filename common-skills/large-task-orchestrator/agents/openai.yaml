interface:
  display_name: "Large Task Orchestrator"
  short_description: "启动确定性 driver，用 BB Worker、Validator 和按需 Judge 持续执行计划"
  default_prompt: "使用 $large-task-orchestrator 在目标 BB 环境启动确定性 driver 执行此计划；先校验计划和路由，保留并发改动，并依据 driver 的退出码和 stderr 处理真实 blocker。"
policy:
  allow_implicit_invocation: false
