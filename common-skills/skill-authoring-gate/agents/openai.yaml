interface:
  display_name: "Skill Authoring Gate"
  short_description: "约束 Skill 调用与依赖，并审查跨 scope 的运行时耦合"
  default_prompt: "Use $skill-authoring-gate before creating, modifying, or reviewing skills."

policy:
  allow_implicit_invocation: true
