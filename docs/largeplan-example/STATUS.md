# 内部文档站一次性令牌登录 — 进展

> 这是供项目参与者阅读的视图，由结构化计划自动生成；请通过计划工具更新内容。

[查看目标与验收](SPEC.md)

## 此刻的判断

尚未开始。第一项结果已经明确，可以直接推进。

最近更新：2026-09-04。

## 正在推进

当前没有正在执行的工作。

## 接下来

- **让员工收到安全的一次性登录邮件**：员工提交企业邮箱后，一分钟内收到同时包含登录链接与验证码的中文邮件；服务端只保存令牌摘要，本阶段不建立登录会话。

## 之后的路线

- **让链接和验证码安全建立现有会话**：员工通过邮件链接或验证码登录并沿用现有会话；过期、错误或被篡改的请求不会建立会话，并提示重新申请。
- **阻止重放与高频滥用**：令牌使用一次后原子失效，高频申请与连续失败被限制，过期数据可幂等清理且不会持续膨胀。
- **让运维看见并收到异常登录**：签发、成功、失败和锁定事件都可从现有运维入口查询；异常失败模式会通知现有值班渠道。
- **用真实场景验收并安全切换登录入口**：同一 acceptance commit 在预发通过全部真实场景，随后向行政团队灰度一天并切为默认入口；共享密码对用户隐藏，但 30 天内仍可在一次发布中恢复。

## 需要关注

目前没有需要人工介入的阻塞或已知残余风险。

## 已经得到的结果

还没有已经验证完成的结果。
