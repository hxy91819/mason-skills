# Repository Agent Rules

## 按任务读取

- 创建、迁移、重命名或修改 skill（含影响行为的脚本、参考文件与配置）前，使用 `skill-authoring-gate`，读取 [编写与触发策略](docs/skill-authoring.md)。同一会话已读且未变化的指南可复用；只有相应分支需要时才读其参考。
- 新增、删除、重命名 skill，或管理 user-scope 软链与来源项目时，读取 [分发与清单规则](docs/skill-distribution.md)。
- 其他设计背景按 [文档索引](docs/README.md) 的任务说明选择；普通编辑无需预读全部文档。

## 范围与完成

以用户要求的结果为完成标准，连续完成已授权的修改、必要验证和下述交付动作。只因新的授权边界、无法推断的用户决策或实际阻塞暂停受影响步骤，并说明具体来源；独立工作继续。

验证针对本次变化：文档检查结构、链接和规则一致性；脚本检查可观察行为。通过后进入交付，后续修改或新失败才触发受影响检查的重跑。`autoreview` 的收敛与范围判断以该技能为准，不设全局调用次数上限。

## 非开源 Skill

含内部账号映射或内网系统信息的 skill 不得提交或推送到本仓库；放在 `~/.agents/skills/<name>/` 的真实目录，排除在软链清单之外，触发策略仍按编写规则配置。发现落在 `common-skills/` 时交付前移出；发现已进入 Git 历史时停止并报告，未经用户授权不得改写历史。

## 交付与权限

当前用户明确是 owner，或当前认证账号与远端归属已证实 owner 身份时，通过必要校验后可直接提交并推送本次授权改动。此权限不覆盖无关内容、删除数据或改写历史；owner 身份无法确认时先完成可审查改动与本地提交，再在推送前向用户确认。

用户指定 PR 交付时，完成分支提交、推送和 PR 核验后回到 `main`，保留 PR 待审阅。其他任务在改动验证通过后按以下默认流程收尾：

1. 检查当前分支、`git status --short` 和 `git worktree list`，核对非开源内容边界与本次文件范围。仅暂存本次任务及用户明确要求交付的现有改动；用户要求“提交当前仓库所有内容”时核对后用 `git add -A`，包括未跟踪文件。
2. 提交并推送到对应远端分支。保留并发无关改动；同一处修改先重读并合并可兼容内容，语义冲突无法判断时才暂停该文件。
3. 若提交不在 `main`，切换到 `main`，优先快进合并任务分支，再推送 `main`；合并使用 `--no-autostash`，冲突时停止并报告。本就在 `main` 时确认已推送即可。
4. 最终停留在 `main`，确认推送成功且 `git status --short` 为空、没有未跟踪文件。未满足时继续处理或明确报告阻塞；干净标准不授权删除、覆盖或提交并发产生的无关工作。

产生副作用的 stash 与 autostash 不可用；需要保全现场时创建本地 commit。Git 退出码 77 的处置见 [共享工作区 Git guard](tools/git-shared-worktree-guard/README.md)。
