interface:
  display_name: "Mermaid Lint (archived)"
  short_description: "已归档：历史 Mermaid 渲染校验流程，不推荐安装"
  default_prompt: "This skill is archived. Do not run it unless the user asked to restore the archived Mermaid lint workflow."

policy:
  allow_implicit_invocation: false
